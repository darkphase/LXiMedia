#!/bin/sh -e

USER=lximediacenter
HOME=/var/lib/$USER

case "$1" in
	configure)
		lximediaserver --daemon `who -m | grep -Eo '^[^ ]+'`
		;;

	abort-upgrade|abort-remove|abort-deconfigure)
		;;

	*)
		echo "postinst called with unknown argument \`$1'" >&2
		exit 1
		;;
esac

#DEBHELPER#
